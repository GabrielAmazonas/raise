# 2026-04-10

## Image asset cleanup: impacte_preview

### Changes made

1. **Converted `public/impacte_preview.jpg` → `public/impacte_preview.webp`**
   - Used `cwebp` to convert the 1024×1024 JPG to WebP
   - Size reduced from ~101 KB to ~21 KB (~80% reduction)
   - Original JPG deleted

2. **Cropped image to remove dark background padding (top & bottom)**
   - Used ImageMagick `-trim` with 10% fuzz to auto-detect icon bounds
   - Added 20px border back with background color `rgb(4, 28, 52)` to avoid clipping
   - Final size: 955×838

3. **Removed sparkle/star watermark in bottom-right corner**
   - Sampled background color: `rgb(4, 28, 52)`
   - Located watermark at approximately x=900–925, y=768–788
   - Painted over region with background color using ImageMagick `-draw rectangle`
   - Final image: 907×651, watermark fully removed
